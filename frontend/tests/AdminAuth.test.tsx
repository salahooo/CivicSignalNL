import {expect,it,vi}from'vitest';import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{AdminAuthProvider,createBasicAuthorization}from'../src/AdminAuth';import{AdminLogin}from'../src/AdminLogin';
it('encodes Unicode credentials as UTF-8 Basic',()=>{const value=createBasicAuthorization('beheer','päss');expect(new TextDecoder().decode(Uint8Array.from(atob(value.slice(6)),c=>c.charCodeAt(0)))).toBe('beheer:päss')});
it('keeps login in memory and logs out',async()=>{vi.spyOn(window,'fetch').mockResolvedValue(new Response(JSON.stringify({authenticated:true,username:'admin',roles:['ADMIN'],authenticationType:'BASIC'}),{status:200,headers:{'Content-Type':'application/json'}}));render(<AdminAuthProvider><AdminLogin/></AdminAuthProvider>);await userEvent.type(screen.getByLabelText('Wachtwoord'),'test');await userEvent.click(screen.getByRole('button',{name:'Inloggen als beheerder'}));expect(await screen.findByText('Beheer actief.')).toBeInTheDocument();expect(localStorage.length).toBe(0);await userEvent.click(screen.getByRole('button',{name:'Uitloggen'}));expect(await screen.findByRole('button',{name:'Inloggen als beheerder'})).toBeInTheDocument()});
